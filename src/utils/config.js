
export default {
    API: "http://localhost:8017",
    OPTION: {
        credentials: 'include',
        headers: {
            'content-type': 'application/json'
        }
    },
    siderMenu: [
        {
            icon: "cloud",
            title: "网站名",
            to: "movie"
        }
    ]
}
